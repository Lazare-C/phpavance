{% extends 'base.html.twig' %}

{% block body %}

    <div class="example-wrapper">


        <h1 class="text-2xl mt-8 mb-2">Ajout massif de films</h1>
        <p class="italicm mb-4">Merci de vous baser sur les exemples de fichiers, de plus cette ajout ne vérifie pas que
            les films existent alors prudence!</p>

        {{ form(form) }}

        <div class="italic text-blue-600">

            <a class="no-underline hover:underline" href="{{ asset('example/bulk.csv') }}">Example CSV file</a><br>
            <a class="no-underline hover:underline" href="{{ asset('example/bulk.json') }}">Example JSON file</a>
        </div>

    </div>
{% endblock %}
