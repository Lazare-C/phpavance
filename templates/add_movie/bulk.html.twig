{% extends 'base.html.twig' %}

{% block body %}

    <div class="example-wrapper">


        <h1 class="text-2xl my-8">Bulk add movie</h1>
        {% form_theme form 'tailwind_2_layout.html.twig' %}
        {{ form(form) }}

        <div class="italic text-blue-600">

            <a class="no-underline hover:underline" href="{{ asset('example/bulk.csv') }}">Example CSV file</a><br>
            <a class="no-underline hover:underline" href="{{ asset('example/bulk.json') }}">Example JSON file</a>
        </div>

    </div>
{% endblock %}
